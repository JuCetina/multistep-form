import styled from "styled-components";

const textColor = "#ABBCFF";

export const StepText = styled.p`
  margin: 0 0 5px;
  font-size: 12px;
  font-weight: 400;
  text-transform: uppercase;
  color: ${textColor};
`;
